class Solution:
    def totalMoney(self, n: int) -> int:
        weeks,days=divmod(n,7)
        total=28*weeks+7*(weeks*(weeks-1)//2)
        total+=days*(1+weeks)+days*(days-1)//2
        return total        
